# Evironn Storefront

This repository contains the Evironn storefront baseline.

## Quality checks

Install dependencies with `npm ci`, then run `npm run gate` for the standard quality gate. Use `npm run gate:full` when browser checks are configured.
