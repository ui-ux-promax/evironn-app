# Phase 5D Planning Brief

## Objective

Prepare the final bounded Phase 5 delivery: synthetic read-only demo-admin, consolidated Evironn visual parity across protected and demo routes, integration/debt closeout, final reviews, the single Phase 5 completion gate, and user acceptance before push/PR authorization.

## Repository state

- Repository: `D:\Projects\evironn`.
- Branch: `phase/05-admin-demo`.
- Delivery base and `origin/dev`: `da5e87e`.
- 5C final reviewed candidate: `c14fd8d`; review closeout documentation before 5D preparation: `9c0c3b0`.
- Streams 5A, 5B, and 5C are complete and user-closed.
- Remain on the existing Phase 5 branch. Do not fetch, switch, reset, rebase, clean, push, open a PR, merge, or start Phase 6 during planning.

## Binding sources

- Master plan: `docs/superpowers/plans/2026-08-20-phase-5-admin-demo.md`, especially 5D.1–5D.9 and sections 6–7.
- Roadmap/status/decisions and `.superpowers/sdd/phase-5-handoff.md` / `.superpowers/sdd/progress.md`.
- Technical source: `D:\Projects\fashion-shop` (read-only).
- Normative visual source: `D:\Новая папка (2)\evironn-clone\src\admin` (read-only).
- Existing Evironn protected admin and demo-admin foundation must be reused; no greenfield rewrite.

## Required planning workflow

1. Luna High prepares a compact current-state evidence bundle.
2. One fresh isolated Sol Medium planner writes the executable 5D plan.
3. One fresh isolated Sol Medium plan reviewer returns Critical/Important/Minor findings.
4. Correct all Critical and Important findings; use one fresh re-review only for material corrections.
5. Stop for user approval before implementation.

All Codex agents use the normal/default service tier; no fast, priority, accelerated, or high-speed mode. Claude Opus is not used unless explicitly requested. Visible agent messages use `caveman ultra`; durable documents use normal technical English.

## Required scope

- Deterministic furniture-aware synthetic fixtures and five public read-only demo routes.
- Demo-local presentation primitives with no protected-admin imports or server/auth/data dependencies.
- Recursive demo isolation and protected/demo navigation parity contracts.
- Exact consolidated Evironn visual parity across all completed `/admin` and `/demo-admin` routes while preserving data, DTO, action, authorization, payment, stock, Cloudinary, and demo-isolation boundaries.
- Legacy Ritm owned-surface cleanup using the exact allowed-hit policy from the master plan.
- Proportional critical Phase 5 E2E, final functional/security reviews, full acceptance matrix, and exactly one full Phase 5 completion gate.
- Push, CI, deployed Preview, PR, and merge remain separate explicit authorization gates. Planning must distinguish local closeout from post-authorization delivery.

## Mandatory debt ledger

- Fix or conclusively diagnose checkout contact-input persistence. Afterward recover disposable COD order creation, safe admin cancellation, and stale-tab conflict evidence through the application/E2E path.
- Reconfirm the non-self last-admin refusal with an isolated fixture.
- Inventory every other debt in STATUS, handoff, task reports, reviews, and accepted unavailable evidence. Assign each item one disposition: fix in 5D, verify in 5D, already resolved with evidence, or explicitly approved Phase 6 transfer.
- Functional/security debt may not silently move to Phase 6. Initial Vercel loading performance is already approved Phase 6 scope. Real YooKassa sandbox smoke remains optional under ADR-020.

## Verification economy

- Use one RED and one GREEN focused command per behavior task where practical.
- Typecheck only at shared-contract checkpoints selected by the executable plan.
- Reviewers reuse fresh focused evidence and do not replay broad suites without a concrete reason.
- Run `npm run format`, `npm run gate`, `npm run build`, and the critical Phase 5 E2E set exactly once at final local closeout, unless final-review remediation invalidates that evidence.
- Keep testing proportional to a portfolio project while preserving ADMIN, payment/stock, Cloudinary, and demo-isolation security boundaries.

## Plan quality requirements

- Build a source-parity matrix before task decomposition: reuse unchanged, adapt, port presentation, or retire with evidence.
- Give exact files, interfaces, commands, expected RED/GREEN evidence, commit subjects, review ranges, and stop gates.
- Avoid placeholders, speculative schema/provider/environment changes, duplicate full-gate runs, and rewrites of working Phase 5 logic.
- The final executable plan should be bounded enough for sequential agent execution and should explicitly separate user visual acceptance from push/PR authorization.

## Protected local files

Do not modify, stage, delete, reset, or clean:

- `docs/superpowers/plans/2026-08-12-phase-2a-executable-storefront-home.md`
- `docs/superpowers/plans/phase-2-task-3-execution.md`
