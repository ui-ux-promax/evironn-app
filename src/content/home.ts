import { FEATURED_PRODUCTS } from '../components/FeaturedProducts';
import { HERO_ROOMS } from '../components/heroRooms';

export const heroRooms = Object.values(HERO_ROOMS);
export const featuredProducts = FEATURED_PRODUCTS;
